const saveImageBtn = document.getElementById('saveImageBtn');
const clearBtn = document.getElementById('clear');

document.addEventListener('layerCreationComplete', () => {
    const clearBtn = document.getElementById('clear');
});
